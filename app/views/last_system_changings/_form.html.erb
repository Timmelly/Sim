<%= form_for(last_system_changing) do |f| %>
  <% if last_system_changing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(last_system_changing.errors.count, "error") %> prohibited this last_system_changing from being saved:</h2>

      <ul>
      <% last_system_changing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :changing_date %>
    <%= f.datetime_select :changing_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
